type Usuario {
  id: Int
  nome: String
  email: String
  telefone: String
  perfil: Perfil
}

type Query {
  usuarios: [Usuario]
  usuario(id: Int): Usuario
}

type Mutation {
  criarUsuario(nome: String, email: String, telefone: String): Usuario!
}